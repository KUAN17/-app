# Changelog

## [1.6.6] - 2026-06-16

### 新增
- **備忘依分類分群**：記帳底部面板的備忘 chip 改從帳本歷史讀取，依目前所選分類篩選，最多顯示 8 筆不重複備忘。切換分類時 chip 即時更換（支出依分類、收入依收入分類、專案依專案分類、轉帳顯示轉帳備忘）。原本的全域 localStorage 備忘清單（`ff_recent_memos`）已移除

### 修正
- **帳戶資金承諾警告（一銀餘額不足提示）**：原邏輯需先手動記撥款轉帳才觸發警告，大多數情況不會出現。改為直接以進行中專案的剩餘預算合計（預算 - 已花費）與帳戶餘額比較，只要餘額低於所有相關專案的剩餘預算加總就顯示警告，並列出影響的專案名稱
- **記帳底部面板日期可點選**：數字鍵盤面板的 📅 日期原本是靜態文字，點了無反應。改為隱藏式 date input，點擊可選擇日期，選完同步更新主畫面日期

## [1.6.2] - 2026-06-15

### 修正
- **台股報價無法取得（TWSE / TPEX 改為 403）**：TWSE 與 TPEX Open API 加了來源限制，直接從瀏覽器 fetch 回 403 Forbidden。改透過 Cloudflare Worker Proxy 轉發請求（帶正確 Referer / User-Agent），瀏覽器只與 Worker 通訊不跨域
- 新增 `cloudflare-worker/twse-proxy.js`，支援 `?target=twse` 與 `?target=tpex`，Worker Edge Cache 1 小時
- `CFG.QUOTE_PROXY` 集中管理 Proxy URL；`investments.js` fetch 改指向 Proxy

### 注意
- 需先部署 `cloudflare-worker/twse-proxy.js` 並將 `js/config.js` 的 `QUOTE_PROXY` 改為實際 Worker URL 才會生效（見 Worker 檔案內說明）

## [1.6.1] - 2026-06-14

### 新增
- **分類圈圖「其他」可展開**：圈圖只顯示前 N 大分類，其餘併入「其他」。現在點「其他」可展開，列出實際包含哪些分類及各自金額／佔比（分析版與卡片版皆支援）

## [1.6.0] - 2026-06-14

### 新增
- **代付補款逐筆精準綁定**：點某筆代付的「記補款 →」時，補款轉帳會記錄結清的支出 ID，Dashboard 依 ID 精準對應——點哪筆就結清哪筆，金額可在數字鍵盤調整做部分補款，剩餘掛在同一筆名下，不會誤扣其他項目。沒有綁定的舊補款／手動轉帳仍以同組 FIFO（先借先還）沖銷作為後備
- **同步補款也綁定**：記帳時開啟「同步補款轉帳」自動產生的補款，一併綁定當下這筆代付支出

### 變更
- **Ledger 表頭補齊**：初始化腳本的表頭原本只到 L 欄，實際資料已寫到 N 欄。現補上 `代付角色`(M)、`代付帳戶`(N)，並新增 `結清代付ID`(O)。讀取範圍 `Ledger!A2:O`、寫入 `Ledger!A:O`

### 注意
- 既有資料的 O 欄為空，舊的代付補款轉帳會自動走 FIFO 後備邏輯，行為與先前一致；新記的補款才會逐筆精準綁定

## [1.5.2] - 2026-06-14

### 修正
- **專案支出編輯時分類不再必填**：專案支出分類為自由輸入、本可留空，編輯視窗卻一律擋下空白分類——現在僅非專案維度才強制必填
- **備忘前導零被吃掉**：備忘輸入「00878」被 Google Sheets 當數字存成「878」。新增 `Utils.sheetText()`，純數字備忘寫入時加 `'` 前綴強制視為文字（讀回不帶前綴），記帳與編輯兩處皆套用

### 變更
- **代付往來改為逐筆記補款**：原本每組「誰欠誰」只顯示加總淨額、按一顆按鈕補全部；現在改為逐筆列出未結清的代付支出（日期、備忘、剩餘金額），各自一顆「記補款 →」只補該筆金額。補款轉帳依「債務人→債權人」FIFO（先借先還）自動沖銷，部分補款顯示剩餘額

## [1.5.1] - 2026-06-14

### 修正
- **編輯帳目主分類被重設為「食物」**：編輯視窗的分類下拉只放預設分類清單，當該筆的分類不在清單內（最常見為專案支出的自訂分類）時無任何選項命中，瀏覽器自動選第一項「食物」——現在自訂分類會補進選項並正確選中，保留原值

## [1.5.0] - 2026-06-14

### 新增
- **代付往來「記補款 →」**：Dashboard 代付往來面板每筆欠款旁新增按鈕，點擊跳轉記帳頁並預填轉帳（債務人 → 債權人、欠款金額、分類自動標記「代付補款」），與帳單頁「前往繳費」同一機制
- **現金／活存代付也能同步補款**：同步補款轉帳不再限信用卡。代付帳戶為現金或活存時，補款目標即為該代付帳戶本身；信用卡則補到綁定的扣款帳戶

### 修正
- **同名帳戶代付選錯**：同一角色下若有同名的信用卡與活存（如阿熊的「台新 Richart」兩種），重新點開代付選單會勾到錯誤的那個——下拉選項值與比對加入帳戶類型（`role||name||type`）區分，`getPaymentInfo` 改以角色＋名稱＋類型精準定位

## [1.4.1] - 2026-06-12

### 修正（程式碼品質）
- **loadIdentity 錯誤處理**：僅在試算表真的不存在時（`Unable to parse range`）才自動建表；其他暫時性錯誤（網路、429、5xx）直接往上拋，讓呼叫方 catch 保留本機既有身份，不再誤把 API 失敗當成「新身份」
- **checkIdentity 重繪**：雲端身份與本機不符（換裝置等）時，即時重繪當前頁，讓視角立即更新
- **代付往來負數修正**：債務人超額補款（`amt < 0`）時翻轉債權/債務方向，門檻從 `> 1` 改為 `Math.abs(amt) >= 1`
- **autoTransfer 無非信用卡帳戶時靜默跳過修正**：`canAuto` 現在額外檢查角色是否有非信用卡帳戶，無則不顯示「同步補款轉帳」選項，避免補款轉帳以空 `transferFrom` 靜默略過
- **代付 UI 抽共用函式**：`buildPaySection(sh)` 與 `buildPayExtra(sh, date, amount)` 取代日常支出、專案支出兩處近乎相同的重複程式碼
- **Utils.identity() 統一存取**：各頁面移除各自的 `identity()` 副本，一律改用 `Utils.identity()`；`CFG.LS_KEYS.IDENTITY` 取代魔法字串 `'ff_identity'`
- **CAT_REPAYMENT 常數**：`'代付補款'` 硬編碼全部換為 `CFG.CAT_REPAYMENT`

## [1.4.0] - 2026-06-12

### 新增
- **使用者身份雲端同步**：身份設定改存到試算表的 `Settings` 工作表（Email ↔ 身份對照），跟著 Google 帳號走——同一個 Gmail 在 Safari、PWA、不同裝置間自動帶入同一身份，不再需要每個環境各設一次
- **新帳號強制完成設定**：第一次用某個 Google 帳號開啟 APP（雲端查無身份）時，自動導向設定頁要求先完成使用者身份設定，且不繼承裝置上前一位使用者的身份
- 設定頁顯示目前登入的 Google 帳號；`Settings` 工作表不存在時自動建立，Apps Script 初始化腳本同步納入

### 注意
- OAuth 權限新增 email（用於辨識登入帳號），既有使用者首次更新後可能需要重新授權一次

## [1.3.3] - 2026-06-12

### 修正
- 底部 Sheet 下滑關閉失效（拖了會彈回）：改用 Pointer Events ＋ 指標捕捉，拖曳不再被瀏覽器當成頁面捲動而中斷；整張 Sheet 空白處（標題、金額區）都可下拖關閉，門檻降為 70px
- 標題列新增「✕」關閉按鈕，點一下即可關閉 Sheet

## [1.3.2] - 2026-06-12

### 修正
- 收入／轉帳底部 Sheet 無法關閉：手機上 overlay 幾乎被 Sheet 蓋住，現在改為往下滑（從灰色 handle 或標題列向下拖）即可關閉，拖超過 90px 自動收起

## [1.3.1] - 2026-06-12

### 修正
- 記帳頁角色卡「⋯」展開後切換頁面再回來仍顯示全部成員：`_showAll` 現在每次進入頁面時自動重置，確保只顯示「自己＋家用」

### 新增
- **日常支出代付**：日常支出底部 Sheet 新增「進階：代付」區塊，與專案支出相同，可選代付帳戶並同步補款轉帳
- **補款轉帳分類**：同步補款自動產生的轉帳，分類欄（F 欄）統一寫入「代付補款」，方便篩選與查核
- **Dashboard 代付往來面板**：自動統計所有未結清的代付金額（代付支出扣除已補款），顯示「X 欠 Y $金額」，身份設定後以「應收/應付」方式呈現

## [1.3.0] - 2026-06-12

### 新增
- **使用者身份**（設定 → 使用者身份）：每台裝置設定自己是誰（阿熊／綺綺／家用視角），記帳與 Dashboard 以「自己＋家用」為主視角
- **記帳頁全面改版**（A＋C 整合版）：
  - 角色大卡片只顯示「自己＋家用」，其他成員收進「⋯」
  - 支出分類圖示直接攤平在主畫面，左右滑動翻頁（每頁 8 格＋分頁點），順序依各角色使用頻率自動排列
  - 「📁 專案」成為分類格之一，取代原「日常/專案」切換；「支出/收入/轉帳」Tab 取消，收入與轉帳改為底部按鈕
  - 點分類後底部 Sheet 一氣呵成：大字金額＋內建數字鍵盤＋帳戶自動帶上次使用＋備忘，送出鈕顯示金額
  - 代付與同步補款收進專案 Sheet 的「進階」摺疊區
- **帳單頁「前往繳費」**：點選跳轉記帳頁並自動帶入轉帳＋該信用卡＋應繳金額；移除無實際作用的「標記已繳」
- **Dashboard 信用卡待繳**：總資產卡片下方顯示「信用卡待繳 -$X」，刷卡未繳的負債一目瞭然
- **Dashboard「我的」視角**：設定身份後預設顯示「自己＋家用」合計，可切換全家總覽

## [1.2.1] - 2026-06-11

### 修正
- 「更新期初餘額」對帳視窗儲存無效：儲存時會被畫面上的舊數值覆蓋，已修正讀取順序，存檔後帳戶列表同步刷新
- 記帳日期格式統一：從 Google Sheets 讀入時即轉為斜線格式（2026/06/10），消除各頁面月份篩選偶發失效的根因
- 帳本月份篩選條件簡化，移除永遠不成立的多餘判斷
- 代付扣款比對加入付款角色：不同角色的同名帳戶（如各自的「現金錢包」）不再被誤扣

### 優化
- Dashboard 帳戶餘額改為單次掃描一次算完全部帳戶（原本每個帳戶各掃一遍帳本、且重複計算兩次），資料量大時明顯加快
- 帳本列表分頁載入：一次顯示 50 筆，底部「載入更多」按鈕逐批載入，筆數多時不再卡頓
- 投資市值與損益改由 APP 以「股數 × 價格」直接計算，不再依賴試算表公式欄位，公式失效或延遲也不影響顯示（按「更新報價」即取得最新行情）
- 移除舊版 Dashboard 殘留的無用樣式

## [1.2.0] - 2026-06-10

### 新增
- Dashboard 全面改版，提供兩種版型，右上角「◐ 分析 / ▦ 卡片」按鈕可即時切換（記憶選擇）
  - **分析版**：支出分類環圈圖（中央顯示總支出）＋ 收入/結餘列 ＋ 分類明細（色點、百分比）＋ 總資產/投資 2 欄小卡
  - **卡片版**：2×2 資訊卡（結餘、總資產、收入/支出、投資損益）＋ 小環圈搭配 Top 3 分類
- 帳戶餘額、進行中專案改為可摺疊區塊，減少捲動長度
- 月/年切換整合進日期導航列，篩選區從兩排縮為一排

### 修正
- Dashboard 帳戶餘額改用統一的 `Store.calcBalance` 計算，正確處理基準日、代付扣款與公積金提撥（先前獨立計算未涵蓋這些情境）

## [1.1.0] - 2026-06-08

### 新增
- 信用卡帳戶可綁定「扣款帳戶」（設定 → 帳戶管理 → 編輯信用卡）
- 支出記帳（專案模式）：代付選信用卡且有綁定扣款帳戶時，自動顯示「同步補款轉帳」開關
- 開啟同步補款後，送出時同步寫入一筆轉帳（費用角色帳戶 → 代付者的扣款帳戶）

### 情境說明
家電專案費用歸屬**家用**，阿熊用信用卡代付。  
記帳時勾選「同步補款轉帳」，系統自動產生兩筆記錄：

1. **支出**：家用 / 家電專案 / 金額 X / 阿熊信用卡（代付）
2. **轉帳**：家用一銀（轉出）→ 阿熊聯邦活存（信用卡扣款帳戶）

阿熊信用卡帳單到期後，從聯邦帳戶繳款，淨結果：
- 阿熊：無實際支出（信用卡 +X，聯邦 +X 再 -X，淨零）
- 家用：有完整支出記錄（專案支出 X，一銀 -X）

# PRD — 家庭財務管理 App（正式版）

| | |
|---|---|
| 版本 | v102（2026-07-06） |
| 分支 | `claude/review-expense-app-requirements-X6uFt`（正式版，GitHub Pages 部署） |
| 後端 | 使用者自有 Google 試算表（Sheets API） |
| 對應文件 | 商轉／SaaS 版 PRD 見 `dev-supabase` 分支的 `PRD.md` |

---

## 1. 產品概述

以「**家庭**」為單位的記帳 PWA。資料存在使用者自己的 Google 試算表，App 不經手任何外部伺服器；可加入手機主畫面、離線開啟。

### 解決的核心痛點（差異化）
市面單人記帳 App（記帳城市、CWMoney、麻布記帳）無法處理家庭最常見的金流：

1. **代付與補款**——「家用的東西刷我的卡」誰欠誰、欠多少，自動追蹤到逐筆結清
2. **信用卡帳單管理**——按結帳週期切帳單、繳費 FIFO 沖銷、多期未繳提醒
3. **分期付款**——一筆消費自動拆 N 期入未來帳單，且可與代付疊加
4. **專案預算**——裝潢、家電等大額支出的預算、進度與資金缺口

### 目標使用者
2-4 人的家庭（夫妻＋共同帳戶），各自有 Google 帳號，共用同一份試算表。

## 2. 目標與非目標

**目標**
- 記一筆日常支出 ≤ 3 秒（進頁即可按數字）
- 家庭內「誰欠誰」永遠有一個正確、可一鍵結清的答案
- 所有畫面上的金額可互相對帳，不出現兩處數字矛盾

**非目標（本版不做）**
- 多家庭/多租戶、訂閱收費、邀請流程（→ SaaS 版）
- 銀行帳戶自動同步、載具/發票匯入
- 預算警示推播

## 3. 角色模型

### 3.1 成員（預算角色）
- 動態清單，存於 `Backend!V:W`，型別分 **personal**（個人）與 **shared**（共享，如「家用」）
- 未設定時自動沿用預設三角色（阿熊/綺綺=personal、家用=shared），零遷移
- 設定頁可新增/改名/刪除成員（上限 20）：
  - **改名連動**：自動改寫帳本（roleOut/roleIn/payRole）、專案歸屬、帳戶設定、投資表、身份綁定
  - **刪除防呆**：帳本或帳戶仍有引用時擋下

### 3.2 登入身份
- Google OAuth 登入；身份（我是哪個成員）存於試算表 `Settings`（Email↔身份），跨裝置自動同步
- 個人身份的預設視角＝「自己＋共享角色」；共享身份＝僅共享角色

## 4. 核心概念

| 名詞 | 定義 |
|---|---|
| 帳戶類型 | 現金／銀行／信用卡／證券帳戶 |
| 交易類型 | 支出／收入／轉帳（15 欄 Ledger，見 §8） |
| 代付 | 支出的費用歸屬（roleOut/accountOut）與實際扣款（payRole/payAccount）分離 |
| 補款 | 分類=「代付補款」的轉帳；以 settleId 逐筆綁定原支出，無綁定則同組 FIFO 沖最舊 |
| 分期 | 一筆信用卡消費拆 N 筆支出，日期逐月遞增；同組共用 ID 前綴 `gid-i期數` |
| 帳單 | 每張信用卡按結帳日切出的週期；狀態＝已繳清/未繳清 |
| 調帳 | 對帳校正自動補記的收入/支出，分類=「調帳」 |

## 5. 功能需求（逐頁）

### 5.1 總覽 Dashboard
- **待辦卡**（頂部）：已出帳且未繳清的信用卡（含天數、多期加註「含 N 期」、一鍵帶全卡未繳合計去繳費）＋未結代付合計（一鍵展開）
- 視角切換（我的/全部/單一成員）、月/年期間切換
- 兩種版型：分析版（環圈圖＋分類清單）/卡片版（2×2）
- **分類固定色**；分類列可點開該分類本期明細（含「其他」子分類）
- 總資產卡：帳戶合計＋**信用卡應繳**（所有未繳清帳單剩餘總和，含未出帳與未來分期）
- 代付往來：逐筆未結代付、「記補款」；分期代付可選「補這期/補全部剩餘期數」
- 帳戶餘額/進行中專案摺疊區；專案超支與資金缺口警示；帳戶「專案剩餘預算 > 餘額」警示
- 年檢視顯示 12 個月收支趨勢
- 空狀態顯示引導 CTA

### 5.2 記帳 Entry
- 進頁自動開表單（帶上次分類），數字鍵盤即位＝三秒記帳；關表單見主畫面（大支出鈕＋收入/轉帳/專案）
- 表單內可切換：**類型 tabs**（支出/收入/轉帳/專案）、**角色 chips**（含🏠共享）、**分類 chips**（橫滑）——切換保留已輸入金額與備忘
- 表單固定版面：金額/鍵盤/送出釘住，僅中段欄位可捲
- **代付**：進階區選任何成員的帳戶；跨成員時可勾「同步補款轉帳」一次寫入支出＋補款（原子寫入）
- **分期**：付款或代付帳戶為信用卡時出現（3/6/12/24 期）；金額均分末期補差、月底夾擠；代付+分期時停用同步補款（由代付往來逐期/全額補）
- **防呆**：收入與轉帳轉出不可選信用卡；補款模式切換類型時警告
- 補款預填（來自 Dashboard/帳單頁）：單筆帶 settleId；全額補款為每期各建一筆轉帳、各自綁定，金額鎖定

### 5.3 帳本 Ledger
- 依月/成員/類型篩選，分頁載入
- 編輯：金額/日期/分類/帳戶/備忘/轉入對象/**代付來源**；settleId 一律保留
- **刪除＝直接執行＋Undo Toast（5 秒復原）**：刪代付支出連帶其補款；刪分期任一期連帶整組＋各期補款（ID 前綴分組，舊資料退回備忘比對）；復原為多列原子寫回

### 5.4 帳單 Billing（信用卡）
- 每卡顯示 本期（進行中）/上期/**更早未繳**（多期未繳的盲區）
- 金額/已繳/狀態一律出自帳單引擎（§6.2）；部分繳費顯示「帳單 X，已繳 Y」
- 「前往繳費」帶**全卡未繳合計**預填轉帳（轉入=卡）

### 5.5 專案 Projects
- 一般專案與貸款計畫（月供/利率/期數/還清日試算、多貸款總覽）
- 預算/已花費/剩餘/已補代付/資金缺口；「明細」列出全部支出（含代付標記）
- 結案＝直接執行＋Undo；改名連動改寫帳本 projectTag

### 5.6 投資 Investments
- 依證券帳戶分組持倉；批次（lots）加倉/FIFO 減倉/清倉；台股報價經 Cloudflare Worker 更新
- 市值/損益由股數×價格前端計算，不依賴試算表公式

### 5.7 設定 Settings
- 身份切換、成員管理（§3.1）
- 帳戶管理：卡片顯示**目前餘額/目前應繳＋最後異動日**（純顯示）；新增可填期初餘額/基準日（信用卡免填）；編輯不含期初；同名信用卡跨成員擋下；刪除有引用時警告；改名全連動
- **對帳校正**：填實際餘額，二模式——補記「調帳」交易（留痕）或直接改期初＋基準日
- 連線設定（Sheet ID/Client ID）、Apps Script 初始化、登出

## 6. 關鍵業務規則

### 6.1 餘額計算（銀行/現金）
`餘額 = 期初 + Σ(基準日起：收入 − 支出 ± 轉帳)`；代付支出只扣 payAccount，費用歸屬帳戶不動。

### 6.2 信用卡（帳單中心模型）
- 卡片**不使用期初餘額與基準日**，一律以帳本全紀錄推算
- **帳單引擎 `Utils.cardBills()`**（單一真相來源）：刷卡按結帳日切期（月底夾擠）；繳費（轉帳入卡）**不看日期、依帳單順序 FIFO 沖最舊**，溢繳滾入下期
- 信用卡應繳 = Σ 未繳清帳單剩餘（含未出帳與未來分期）；待辦卡只列「已出帳且未繳清」
- 收入入卡不影響卡債；卡不可作轉出帳戶

### 6.3 代付往來
`未結金額 = 代付支出 −（settleId 綁定補款 ＋ 同組 FIFO 補款）`，逐筆列示。

### 6.4 統計排除
當月收支/分類圖排除分類=「信用卡費」（還債，刷卡時已計）與「調帳」（校正非消費）；餘額計算不受影響。

### 6.5 專案
`已花費=Σ專案支出`；`已補代付=專案轉帳＋綁定補款`；`資金缺口=max(0, 代付支出−已補款)`。

## 7. 非功能需求

### 7.1 PWA 與快取
- Service Worker **network-first**：部署即生效，離線退回快取；資料快取 TTL 5 分鐘
- 版本以 CACHE 常數管理（現為 v102）

### 7.2 資安
- **XSS**：所有使用者輸入（備忘/帳戶/專案/成員/標的名）插入 HTML 前經 `Utils.esc()` 跳脫；selector 用 `CSS.escape`
- **Token**：OAuth access token 存 sessionStorage（關 App 失效，重啟靜默換發）；登出撤銷
- 資料僅在使用者授權的試算表與瀏覽器本機，無第三方伺服器

### 7.3 多裝置並發
- 帳本編輯/刪除前以 `verifyLedgerRows()` 驗證列位 id，位移即中止並重載
- 改名連動前強制重新載入；寫入採單請求多列（原子）
- Sheets API 429/5xx 自動退避重試 2 次

### 7.4 已知限制
- 驗證與寫入間仍有毫秒級競態窗（徹底解法在 SaaS 版資料庫層）
- Sheets API 配額（每使用者每分鐘 60 讀/60 寫）為規模上限
- iOS PWA 冷啟動若靜默換發失敗需手動點登入

## 8. 資料模型（Google Sheets）

- **Ledger A:O（15 欄）**：id、roleOut、dimension(日常/專案)、projectTag、type、category、memo、date、amount、accountOut、roleIn、accountIn、payRole、payAccount、settleId
  - id：base36；分期為 `gid-i期數`
- **Projects A:N**：狀態/名稱/預算/…/月供/利率/起始/期數/歸屬角色/預設帳戶
- **Investments A:K**：角色/帳戶/代號/名稱/股數/均價/…/現價
- **Backend L:T**：帳戶設定（角色/名稱/期初/基準日/用途/類型/帳單日/截止日/扣款帳戶）；**V:W**：成員（名稱/類型）
- **Settings A:B**：Email↔身份

## 9. 待辦與後續

- [ ] 記帳頁視覺改版（定案版面：角色日期→付款方式金額→類型→分類5格→鍵盤→完成；深色 A 版風格）——已定稿未實作
- [ ] store.js 計算邏輯單元測試（SaaS 遷移前的數字保證）
- 商轉（多租戶/訂閱/Supabase）：見 `dev-supabase` 分支 `PRD.md`
